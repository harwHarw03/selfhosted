server:
  port: 8084
  cacheDir: /home/filebrowser/data/tmp
  sources:
    - path: /srv
      name: Homelab Storage
      config:
        defaultEnabled: true

auth:
  adminUsername: admin
  methods:
    password:
      enabled: true
      minLength: 12
      signup: false
