# PhotoPrism and MariaDB versions. Test upgrades before changing these values.
PHOTOPRISM_VERSION=240915
MARIADB_VERSION=11.8
TZ=Asia/Jakarta

# Generate unique values with: openssl rand -hex 32
PHOTOPRISM_ADMIN_PASSWORD=replace_with_random_admin_password
PHOTOPRISM_DATABASE_PASSWORD=replace_with_random_database_password
MARIADB_ROOT_PASSWORD=replace_with_random_root_password

PHOTOPRISM_SITE_URL=http://10.10.20.102:2342/
